# @aivo/brain-model

Utilities and helpers for mapping learner grade to grade band and computing difficulty recommendations.

Example:

```ts
import { getGradeBand, getDifficultyRecommendations } from '@aivo/brain-model';
```
