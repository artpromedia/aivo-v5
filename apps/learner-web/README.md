# learner-web

Next.js learner-facing web application (PWA). Uses App Router and Tailwind. For local dev:

```powershell
pnpm dev --filter=apps/learner-web...
```

This app consumes `@aivo/ui` and `@aivo/brain-model` from the workspace.
