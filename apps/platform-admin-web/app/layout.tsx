export const metadata = {
  title: "Platform Admin",
  description: "AIVO Platform Admin UI"
};

export default function RootLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
