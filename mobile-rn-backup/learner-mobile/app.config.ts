import { ExpoConfig } from "expo/config";

const config: ExpoConfig = {
  name: "AIVO Learner Mobile",
  slug: "aivo-learner-mobile",
  scheme: "aivo",
  platforms: ["ios", "android", "web"]
};

export default config;
