# learner-mobile

Expo React Native app for learners. To run locally:

```powershell
cd mobile/learner-mobile
pnpm install
pnpm start
```

Note: pin `@types/react@18.x` is recommended for react-native compatibility.
