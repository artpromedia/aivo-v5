# model-dispatch service

Fastify service responsible for dispatching LLM requests to multiple providers with failover.

Local dev:

```powershell
pnpm dev --filter=@aivo/model-dispatch-service...
```
