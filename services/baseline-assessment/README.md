# baseline-assessment service

Generates baseline assessment items using the model-dispatch service. Fastify based.

Local dev:

```powershell
pnpm dev --filter=@aivo/baseline-assessment-service...
```
